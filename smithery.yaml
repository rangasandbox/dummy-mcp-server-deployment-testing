# Smithery configuration file: https://smithery.ai/docs/build/project-config

startCommand:
  type: stdio
  commandFunction:
    # A JS function that produces the CLI command based on the given config to start the MCP on stdio.
    |-
    (config) => ({ command: 'node', args: ['build/index.js'], env: { SCREENSHOTONE_API_KEY: config.screenshotoneApiKey } })
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    required:
      - screenshotoneApiKey
    properties:
      screenshotoneApiKey:
        type: string
        description: Your ScreenshotOne API key
  exampleConfig:
    screenshotoneApiKey: YOUR_SCREENSHOTONE_API_KEY
